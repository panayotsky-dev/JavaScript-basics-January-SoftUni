function date (input){
    let den = input[0];
    let mesec = input[1];
    let year = input [2];
    let result = "The date is " + den + "/" + mesec + "/"+  year + "";
    console.log(result);
}
date([2,5,2021]);
